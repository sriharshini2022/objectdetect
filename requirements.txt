# --- Core Streamlit ---
streamlit>=1.36.0

# --- Object Detection (YOLOv5-compatible stack) ---
torch==2.0.1
torchvision==0.15.2
torchaudio==2.0.2

# --- Essential Libraries ---
opencv-python-headless==4.8.1.78
Pillow==9.5.0
numpy==1.24.3
pandas==2.0.3
tqdm==4.65.0
matplotlib==3.7.2
seaborn==0.12.2
requests==2.31.0

# --- Optional: avoid ultralytics git install to keep it lightweight ---
# Instead, YOLOv5 will be loaded via torch.hub
# (no need to install ultralytics manually)
